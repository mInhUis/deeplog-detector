# Cloud Log Analytics & Incident Response — Dependencies
# Install: pip install -r requirements.txt

# Core
pandas>=2.0
torch>=2.0
numpy>=1.21.0

# Testing
pytest>=7.0

# Phase 4 — Llama-3 Responder (GPU dependencies, install manually)
# These require CUDA and are not pip-installable on all platforms.
# Install when GPU hardware is available:
#
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install bitsandbytes>=0.43.0
#   pip install transformers>=4.40.0
#   pip install accelerate>=0.30.0
